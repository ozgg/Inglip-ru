<% content_for :meta_title, t('.title', name: @entity.name, page: current_page) %>

<article class="entity-page">
  <h1><%= t('.heading', name: @entity.name) %></h1>

  <ul class="actions">
    <li><%= return_icon(admin_lexeme_group_path(@entity.id)) %></li>
  </ul>

  <%= paginate @collection %>
  <%= render partial: 'admin/lexemes/list', locals: { collection: @collection } %>
  <%= paginate @collection %>
</article>