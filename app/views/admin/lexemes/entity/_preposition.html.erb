<%
  handler = Biovision::Components::Words::LexemeHandler[entity]
  flags = Biovision::Components::Words::PrepositionHandler.lexeme_data_flags
  enums = Biovision::Components::Words::PrepositionHandler.lexeme_data_enum
%>

<section class="lexeme-data">
  <h2><%= t('admin.lexemes.form.lexeme_data') %></h2>

  <%=
    render(
        partial: 'admin/lexemes/entity/lexeme_flags',
        locals: { handler: handler, flags: flags }
    )
  %>

  <%=
    render(
        partial: 'admin/lexemes/entity/lexeme_enums',
        locals: { handler: handler, enums: enums }
    )
  %>
</section>

<%=
  render(
      partial: 'admin/lexemes/entity/preposition/wordforms',
      locals: { handler: handler }
  )
%>
