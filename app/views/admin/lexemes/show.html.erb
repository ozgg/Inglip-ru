<% content_for :meta_title, t('.title', name: @entity.name, page: current_page) %>

<article>
  <h1><%= @entity.name %></h1>

  <ul class="actions">
    <li><%= return_icon(admin_lexemes_path) %></li>
  </ul>
</article>
