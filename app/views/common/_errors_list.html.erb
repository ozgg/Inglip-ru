<% if entity.errors.any? %>
    <div class="errors">
      <h2><%= t('error', count: entity.errors.count) %>:</h2>

      <ul>
        <% entity.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>
