<fieldset class="lexeme-wordforms">
  <legend><%= t('lexemes.form.wordforms') %></legend>

  <dl class="fields">
    <% Biovision::Components::Words::PrepositionHandler.wordform_flags.each do |key, flag| %>
      <%=
        render(
            partial: 'lexemes/form/wordform_list_flag',
            locals: { handler: handler, key: key, flag: flag }
        )
      %>
    <% end %>
  </dl>
</fieldset>
