<% content_for :title, t('.title', word: @noun.nominative) %>
<% content_for :title, t('.description', word: @noun.nominative) %>

<article class="word">
  <header>
    <h1><%= t('.header') %></h1>
    <nav>
      <ul>
        <li><%= link_to t('to_list'), nouns_path %></li>
        <li><%= link_to t('to_view'), @noun %></li>
      </ul>
    </nav>
  </header>

  <%= render 'form' %>
</article>