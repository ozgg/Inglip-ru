<% content_for :title, t('.title') %>
<% content_for :title, t('.description') %>

<article class="word">
  <header>
    <h1><%= t('.header') %></h1>
    <nav>
      <ul>
        <li><%= link_to t('to_list'), posts_path %></li>
        <li><%= link_to t('to_view'), @post %></li>
      </ul>
    </nav>
  </header>

  <%= render 'form' %>
</article>